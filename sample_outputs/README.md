Real output from a live run.

Command:
  bioagenthub-crawl --query "PETase depolymerase" --max 2 --download 1 --out crawler_outputs

Files:
- papers.json (real metadata)
- download_log.json (download status)

PDFs are not included in the repo to avoid licensing issues; they are downloaded at runtime.
